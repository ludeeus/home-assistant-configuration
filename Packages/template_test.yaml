switch:
  - platform: template
    switches:
      skylight:
        friendly_name: 'Skylight'
        value_template: "{{ is_state('switch.rf02', 'on') }}"
        turn_on:
          service: switch.turn_on
          entity_id: switch.rf02
        turn_off:
          service: switch.turn_on
          entity_id: switch.rf02
